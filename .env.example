## Docker Compose local overrides
# Copy to `.env` and tweak as needed.

# Avoid conflicts with other stacks on your machine
API_PORT=8001
DB_PORT=5433

# Admin UI (keep 3000 unless it conflicts)
ADMIN_PORT=3000

# Attachments v2 (MinIO/S3) - optional, defaults are safe for local docker compose.
# MINIO_PORT=9000
# MINIO_CONSOLE_PORT=9001
# MINIO_ROOT_USER=minioadmin
# MINIO_ROOT_PASSWORD=minioadmin
# S3_ENDPOINT_URL=http://localhost:9000
# S3_ACCESS_KEY_ID=minioadmin
# S3_SECRET_ACCESS_KEY=minioadmin
# S3_BUCKET=attachments
# S3_REGION=us-east-1
# S3_USE_SSL=0

# AI provider defaults (optional).
# You can override per-company from Admin: System -> Config -> AI Policy.
AI_PROVIDER=openai
AI_DEFAULT_MODEL=gpt-4o-mini
# OPENAI_API_KEY=...

# Edge -> Cloud sync (optional, for on-prem edge nodes).
# Configure these ONLY on the store/on-prem node that should replicate to the cloud.
# The cloud instance should set EDGE_SYNC_KEY (same shared secret) to accept imports.
#
# EDGE_SYNC_TARGET_URL=https://your-cloud-domain.com
# EDGE_SYNC_KEY=replace-with-strong-shared-secret
# EDGE_SYNC_NODE_ID=beirut-store-1
