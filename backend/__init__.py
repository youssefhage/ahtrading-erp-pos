"""
Backend package marker.

We keep this file so `import backend...` works reliably in all environments
(including pytest collection and some tooling that doesn't handle namespace
packages consistently).
"""

