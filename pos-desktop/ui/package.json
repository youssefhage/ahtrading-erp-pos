{
  "name": "codex-pos-desktop-ui",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "compile:svelte": "node scripts/compile-svelte.mjs",
    "dev": "npm run compile:svelte && vite",
    "build": "npm run compile:svelte && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "svelte": "^5.25.0"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
