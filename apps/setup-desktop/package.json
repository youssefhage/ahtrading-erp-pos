{
  "name": "melqard-setup-desktop",
  "private": true,
  "version": "0.0.6",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "node scripts/tauri-build.mjs",
    "build:dmg": "node scripts/tauri-build.mjs --bundles dmg",
    "build:windows": "node scripts/tauri-build.mjs --bundles nsis,msi"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-updater": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
